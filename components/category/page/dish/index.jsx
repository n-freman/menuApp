import { View, Text, Image } from 'react-native';
import styles from './style';

const Dish = ({item}) => {
    return (
        <View
            style={styles.dishWrapper}
        >
            <Image
            
            />
        </View>
    );
}

export default Dish;
